if [ ! -z "$CONFIG_SSH_AUTHORIZED_KEYS" ]
then
    echo "> Configuring SSH authorized keys"    
    mkdir -p $MOUNT/.ssh
    echo $CONFIG_SSH_AUTHORIZED_KEYS | base64 -d >$MOUNT/.ssh/authorized_keys
fi

