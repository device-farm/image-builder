#!/bin/bash

# https://linux-sunxi.org/Bootable_SD_card#Bootloader

set -e

echo "> Creating file systems..."
mkfs.ext4 -F -L ROOT part-root
#mkfs.ext4 -F -L DATA part-data

mount part-root mount-root

cp -r root/* mount-root

umount mount-root

sync
echo "> Done."
